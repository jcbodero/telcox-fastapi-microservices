apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-gateway-mock
  namespace: {{ .Values.namespace.name | quote }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: payment-gateway-mock
  template:
    metadata:
      labels:
        app.kubernetes.io/name: payment-gateway-mock
    spec:
      containers:
        - name: payment-gateway-mock
          image: "{{ .Values.paymentGateway.image.repository }}:{{ .Values.paymentGateway.image.tag }}"
          imagePullPolicy: {{ .Values.paymentGateway.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.paymentGateway.service.targetPort }}
          resources:
            {{- toYaml .Values.paymentGateway.resources | nindent 12 }}

replicaCount: 1

image:
  repository: ghcr.io/jcbodero/telcox-kyc-identity-mock
  tag: latest
  pullPolicy: IfNotPresent

imagePullSecrets: []

basePath: /kyc-identity-mock

service:
  type: ClusterIP
  port: 80
  targetPort: 8013

ingress:
  enabled: false
  className: traefik
  host: reto1.telcox.site
  tls:
    enabled: false
    secretName: telcox-kyc-identity-mock-tls
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"

resources:
  requests:
    cpu: 20m
    memory: 96Mi
  limits:
    cpu: 80m
    memory: 160Mi

replicaCount: 1

image:
  repository: ghcr.io/jcbodero/telcox-provisioning-service
  tag: latest
  pullPolicy: IfNotPresent

imagePullSecrets: []

basePath: /provisioning-service

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

ingress:
  enabled: false
  className: traefik
  host: telcox.duckdns.org
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: duckdns

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi


